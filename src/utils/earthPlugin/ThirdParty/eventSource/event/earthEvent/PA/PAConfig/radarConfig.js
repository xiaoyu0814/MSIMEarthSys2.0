import store from '@/store'
export default function () {
  // 在PA事件内判断是否为地面雷达目标，如果是则添加到 当前仿真端地面雷达类型使用type值： EARLY WARNING SENSOR’
  const collectGroundRadarName = (json) => {
    //
  }

  return { collectGroundRadarName }
}
